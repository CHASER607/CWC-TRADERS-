# CWC Traders Portal

A lightweight browser-based trading portal with:

- Live-style watchlist cards for key instruments.
- Quick order entry (buy/sell + quantity).
- Portfolio summary updates after each trade.
- Recent orders table with fill details.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
